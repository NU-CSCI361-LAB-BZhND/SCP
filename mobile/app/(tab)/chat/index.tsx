import { Text, View } from 'react-native';

export default function Chat() {
  return (
    <View
      style={{
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>chat</Text>
    </View>
  );
};
